# ============================================================
# Pinned to a resolver-verified, mutually-compatible version
# set (confirmed via `pip install --dry-run`) to avoid slow
# backtracking / dependency conflicts during `docker build`.
# Do not loosen these unless you re-verify compatibility.
# ============================================================

# ---- Core web framework ----
fastapi==0.115.14
uvicorn[standard]==0.30.6
starlette==0.46.2
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4

# ---- Database ----
sqlalchemy==2.0.51
asyncpg==0.29.0
alembic==1.18.5

# ---- Auth ----
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.32

# ---- HTTP / utils ----
httpx==0.27.2
tenacity==9.1.4
websockets==16.1.1
python-dotenv==1.2.2
loguru==0.7.3

# ---- LLM orchestration ----
langchain==0.3.30
langchain-core==0.3.86
langchain-community==0.3.31
langchain-groq==0.2.5
langchain-openai==0.2.14
langchain-text-splitters==0.3.11
langgraph==0.2.76
langgraph-checkpoint==2.1.2
langgraph-sdk==0.1.74
langsmith==0.10.10

# ---- Vector DB ----
qdrant-client==1.12.1

# ---- Local embeddings (no API key, no torch — ONNX runtime only, much
# smaller/faster download than sentence-transformers+torch) ----
fastembed==0.8.0

# ---- Search ----
tavily-python==0.7.26

# ---- Scraping ----
beautifulsoup4==4.15.0
playwright==1.61.0

# ---- MCP ----
mcp==1.12.4

# ---- Tests ----
pytest==9.1.1
pytest-asyncio==1.4.0

email-validator==2.0.0